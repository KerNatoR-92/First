# 잡지식탁 쇼츠 템플릿 (blanket-smell-shorts / rain-smell-shorts 기반)

> 트리거: **"잡지식탁 쇼츠 폼으로 영상 하나 만들어줘"**
> 이 문서에 정의된 레이아웃·타이포·색·오디오·모션 규격을 그대로 재현할 것.
> 실제 참조 프로젝트: `videos/blanket-smell-shorts/` (최신), `videos/rain-smell-shorts/` (원형).

---

## 1. 캔버스

- 해상도: **1080 × 1920** (세로 쇼츠)
- 프레임레이트: **30fps**
- 배경 컬러: `#05060a`
- 폰트 스택: `"Noto Sans CJK KR", "Noto Sans KR", "Noto Sans JP", sans-serif`
- 스무딩: `-webkit-font-smoothing: antialiased`

## 2. 세로 3분할 레이아웃 (rain-video-final 템플릿)

| 밴드 | 위치 | 크기 | 역할 |
|---|---|---|---|
| 상단 검정 | `top: 0` | `height: 460px` | 채널 아이콘 + 고정 제목 |
| 중앙 사진 | `top: 460` | `height: 1060px` | 씬별 배경 이미지 (ken burns) |
| 하단 검정 | `bottom: 0` | `height: 400px` | 자막 영역 |

### 상단 (titlebar)
- `.brand` : `top: 40, left: 60, height: 160, gap: 22`
  - `.icon` : **160 × 160 원형 아이콘** (제목 폰트 82px 기준 두 글자 폭)
    - `box-shadow: 0 0 22px rgba(255,232,107,0.28), 0 4px 18px rgba(0,0,0,0.55)`
    - 소스: `public/brand/channel-icon.png` (루트의 `잡지식탁_아이콘_260815.png`을 프로젝트로 복사)
  - **아이콘 옆 텍스트 없음** (아이콘만)
- `.title` : `top: 224, bottom: 40`, 세로·가로 **중앙정렬** (`align-items: center`, `text-align: center`, `justify-content: center`)
  - `font-size: 82`, `font-weight: 900`, `letter-spacing: -0.035em`, `line-height: 1.08`
  - 두 줄 강제: `flex-direction: column`, 각 줄을 `<span class="line">`로 감싸기
  - 강조어에 `<span class="accent">` (색상 `#ffe86b`) — **제목 한정**

### 중앙 (stage)
- 씬은 `.scene` 클래스, `position: absolute; top: 460; width: 1080; height: 1060`
- `.bg` : `background-size: cover; background-position: center` — 씬별 이미지
- `.veil` : 하단 어두운 그라디언트 (판독성 확보)

### 하단 (capbar + capline)
- `.capbar` : 하단 400px 검정
- `.capline` : `bottom: 280, font-size: 62, font-weight: 800, line-height: 1.22, letter-spacing: -0.025em, white-space: nowrap, word-break: keep-all, text-align: center`
- **자막 색상은 흰색(`#ffffff`)으로 통일** — `.accent` 노랑 강조는 **자막에 쓰지 않는다** (제목에서만 사용). 자막 안에 `<span class="accent">`가 있어도 흰색으로 렌더되도록 `.capline .accent { color: #ffffff; }`로 무력화.
- **자막 한 줄 원칙**: 폰트 62px + nowrap일 때 대략 **한글 17자 이하**를 넘지 않도록 스크립트를 분할. 20자 이상이면 자막 클립을 나눔.

## 3. 색상

- 배경: `#05060a`
- 본문/제목 텍스트: `#ffffff`
- 강조색 (`.accent`): `#ffe86b` — **제목(`.title` 하위)에서만 사용**
- 자막(`.capline` 하위)은 강조어 포함 전체 `#ffffff` 유지
- 텍스트 그림자: `0 2px 12px rgba(0,0,0,0.7)`

## 4. 오디오

- **나레이션**: `narration.wav` (Whisper로 word-level 트랜스크립트 뽑아 자막 타이밍 맞춤)
  - 원본 mp3를 `ffmpeg`로 `dynaudnorm + loudnorm -16 LUFS` 정규화
- **BGM**: `public/bgm/wildfire-60s.m4a` (Wildfire · Jessie Villa 인스트루멘털)
  - Demucs `htdemucs` two-stem으로 보컬 제거
  - `data-volume="0.5376"` (rain 영상 최종 0.672 × 0.8 = 0.5376)
- 나레이션 트랙 `data-track-index="10"`, BGM `data-track-index="11"`

## 5. 페이드아웃 & 총 길이 규칙

- 페이드아웃 요소: 전체 캔버스 덮는 검정 div, opacity 0→1, **4초 duration**
- 시작 시점: **나레이션 끝 - 1.2초쯤** (뒷부분 공백 흡수)
- **root duration = 페이드아웃 종료 시각으로 정확히 맞춤** — 페이드 끝난 뒤에 root가 남지 않도록. 남기면 fade-out 클립이 unmount되어 마지막 씬이 다시 튀어나옴.
- 모든 풀-런 요소(`.stage`, `.titlebar`, `.capbar`, progress, BGM)의 `data-duration`을 root와 같은 값으로. 마지막 씬 duration도 페이드 종료 시각까지 이어지게.

## 6. 씬 & 애니메이션

- 씬 개수: 대략 **6~8개**, 트랙 인덱스는 1과 2를 번갈아 씀 (교차 편집)
- 각 씬에 `sceneKB(id, start, dur)` 헬퍼로 ken burns 적용:
  - opacity 0→1, 0.55s ease `power2.out`
  - scale 1.05→1.14, y +20→-20, 씬 전체 duration, ease `sine.inOut`
  - 씬 끝 min(0.55, dur*0.18)s에 opacity 페이드아웃

## 7. 자막 (caption) 규칙

- 클립 아이디: `cap-0`, `cap-1`, ... (분할 추가시 `cap-28`~ 순차 부여)
- 트랙 인덱스는 3과 4를 번갈아 사용해 붙는 자막이 겹치지 않게
- 애니메이션: 개별적으로 `y: 14 → 0`, opacity 0→1 in (min 0.18s), out은 `y: 0 → -8`, opacity 1→0 (min 0.14s)
- JS 루프는 **`document.querySelectorAll('.capline').forEach`** (ID 하드코딩 금지)
- 나레이션 pitch-preserving speed는 **1.0x 기본** (필요시 1.1x, 그때는 BGM만 원속도 유지)

## 8. 상단 진행바

- 나레이션 종료 시각까지 채우는 `scaleX 0→1` 리본 (`.progress`)
- 높이 6px, 그라디언트 `#ff3d68 → #ffe86b`

## 9. 브랜드 아이콘 미세 애니메이션

- `.titlebar .icon` scale 0.96↔1.04, opacity 0.9↔1, 1.4s sine.inOut yoyo
- `repeat`: `Math.max(0, Math.floor(rootDuration/2.8) - 1)`

## 10. 썸네일 (thumbnail.html)

- 별도 1080×1920 정지 이미지용 HTML
- 중앙 큰 제목 (142~158px, `.accent`로 핵심어 강조), 좌상단 브랜드
- 배경은 첫 씬 이미지에 강한 어둠 veil을 씌워 텍스트 판독성 확보

## 11. 프로젝트 구조

```
videos/<프로젝트>-shorts/
  index.html            # 메인 컴포지션
  thumbnail.html        # 유튜브 썸네일
  package.json          # hyperframes CLI 스크립트
  meta.json             # id, name
  narration.wav         # 정규화된 나레이션
  transcript.json       # Whisper word-level 결과
  public/
    bg/                 # 씬별 배경 이미지 (Unsplash 등)
    bgm/                # 인스트루멘털 BGM
    brand/channel-icon.png
    vendor/gsap.min.js
  renders/
    video.mp4           # 원본 렌더
    video-upload.mp4    # 유튜브 업로드용 압축본 (< 30MiB)
    video-compressed.mp4  # 채팅 프리뷰용 (540×960)
```

## 12. 새 영상 만들 때 절차

1. `videos/<주제>-shorts/` 로 hyperframes 프로젝트 스캐폴드
2. 나레이션 mp3 준비 → `ffmpeg` 정규화 → `narration.wav`
3. `faster-whisper` (small · int8 · Korean)로 `transcript.json` 생성
4. 스크립트를 문장 단위 자막으로 나누되 **한 줄 17자 초과 금지**
5. Unsplash에서 씬별 배경 이미지 다운로드 → `public/bg/`
6. 루트의 `잡지식탁_아이콘_260815.png` → `public/brand/channel-icon.png`로 복사
7. 참조 프로젝트 `videos/blanket-smell-shorts/index.html`을 템플릿으로 복사 후 텍스트·타이밍만 교체
8. `npm run check` → `npm run render`
9. 렌더본을 `video.mp4`로 이름 변경, `video-upload.mp4`(CRF22)와 `video-compressed.mp4`(540p CRF30) 함께 생성
10. 커밋 + 지정 브랜치로 push

## 13. 자주 놓치는 것 (체크리스트)

- [ ] root duration이 페이드아웃 종료 시각과 정확히 일치하는가?
- [ ] `.stage`, `.titlebar`, `.capbar`, `#el-progress`, BGM의 `data-duration`도 root와 일치하는가?
- [ ] 마지막 씬의 duration이 페이드 종료 시각까지 이어지는가?
- [ ] 자막 한 줄이 17자를 넘지 않는가?
- [ ] `white-space: nowrap`이 걸린 상태에서 실제 렌더 폭이 1032px를 넘지 않는가?
- [ ] BGM 볼륨이 0.5376인가?
- [ ] 브랜드 아이콘이 160×160으로 좌상단에 있고 옆 텍스트는 없는가?
- [ ] 제목이 가로·세로 중앙정렬로 렌더되는가? (`align-items: center` + `text-align: center` + `justify-content: center`)
- [ ] 자막 색상이 전부 흰색인가? (`.capline` 안의 `.accent`도 흰색으로 무력화)
- [ ] 자막 루프가 `.capline` 클래스로 순회하는가? (인덱스 하드코딩 아님)
