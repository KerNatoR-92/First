# 잡지식탁 쇼츠 양식 (Series Spec)

> 트리거: **"잡지식탁 쇼츠 폼으로 영상 하나 만들어줘"**
> 새 주제·제목이 와도 아래 규격을 그대로 재사용해 시리즈 일관성을 유지한다.
> 참고 구현: `videos/wrinkled-fingers-shorts/` — 이 문서는 그 구현을 코드-레벨로 정리한 시리즈 원본.

---

## ⛔ 절대 금기 (렌더 시작 전 반드시 확인)

이 네 항목 중 하나라도 어긴 결과물은 **재작업 사유**다. 참조 구현 파일(`videos/wrinkled-fingers-shorts/`)을 그대로 복사하면서 무의식적으로 딸려 오는 경우가 많으니 명시적으로 지워야 한다.

1. **나레이션 트랙(track-index 10)에 `afade` 또는 볼륨 감쇠를 절대 걸지 않는다.** 나레이션은 §10-1 loudnorm 파이프라인 하나로만 처리하고, 그 뒤엔 어떠한 볼륨 자동화·페이드도 붙이지 않는다. 페이드인/페이드아웃은 **BGM 트랙(11번)에만** 건다 (§10-2). 이걸 지키지 않으면 시청자가 결말을 못 알아듣는다.
2. **썸네일은 렌더 영상에서 프레임을 추출하지 않는다.** `renders/thumbnail.jpg`를 생성하지도, 저장소에 커밋하지도 않는다. 첫 프레임 추출(`ffmpeg -ss ... -frames:v 1 renders/thumbnail.jpg`) 명령을 절대 실행하지 않는다. 대신 **첫 씬의 원본 사진(예: `public/bg/scene-1.jpg`)** 을 그대로 사용자에게 썸네일 소스로 전송한다. 시청자가 첫 인상을 받는 이미지이므로 씬 사진 라이브러리 중 주제에 가장 잘 맞는 원본을 골라 보낸다.
3. **자막 텍스트에서 물음표(`?`)만 유지**하고 나머지 문장부호(`.` `,` `!` `…` 등)는 전부 제거한다. 원본 스크립트에 있어도 예외 없이 삭제.
4. **자막 컷은 사용자가 넘긴 스크립트의 줄바꿈 그대로**. 한 줄 = 한 자막 클립. 임의 분할·합치기 금지.

각 항목의 상세 규칙과 검증 명령은 아래 해당 절(§9, §10, §12~14)에 명시.

---

## 1. 캔버스

| 항목 | 값 |
|---|---|
| 해상도 | **1080 × 1920** (세로 쇼츠) |
| 프레임레이트 | **30fps** |
| 배경 컬러 | `#05060a` |
| 총 길이 계산 | `root duration = narration_duration - 1.2 + 4.0` (페이드아웃 종료 시각과 정확히 일치) |
| 페이드아웃 시작 | `narration_end - 1.2` |
| 페이드아웃 duration | **4.0초** |

`<html data-resolution="portrait">`, `<meta name="viewport" content="width=1080, height=1920">`.

---

## 2. 폰트 (필수: 로컬 임베드)

렌더 컨테이너의 헤드리스 크로미움에는 한글 폰트가 설치돼 있지 않아 시스템 폴백이 되면 서체가 달라진다. **반드시 Noto Sans KR woff2를 프로젝트에 넣고 `@font-face`로 로컬 참조**.

```
public/fonts/
  NotoSansKR-Bold.woff2   (weight 700)
  NotoSansKR-Black.woff2  (weight 800~900)
```

다운로드 방법 (한 번만 하면 됨):

```bash
curl -sSL -A "Mozilla/5.0" -o NotoSansKR-Black.otf \
  https://raw.githubusercontent.com/notofonts/noto-cjk/main/Sans/SubsetOTF/KR/NotoSansKR-Black.otf
curl -sSL -A "Mozilla/5.0" -o NotoSansKR-Bold.otf \
  https://raw.githubusercontent.com/notofonts/noto-cjk/main/Sans/SubsetOTF/KR/NotoSansKR-Bold.otf
python3 -c "
from fontTools.ttLib import TTFont
for n in ['NotoSansKR-Black','NotoSansKR-Bold']:
    f=TTFont(n+'.otf'); f.flavor='woff2'; f.save(n+'.woff2')"
```

`@font-face` 선언(6개, 세 family로 세팅):

```css
@font-face { font-family: "Noto Sans KR"; src: url("public/fonts/NotoSansKR-Bold.woff2") format("woff2"); font-weight: 700; font-display: block; }
@font-face { font-family: "Noto Sans KR"; src: url("public/fonts/NotoSansKR-Black.woff2") format("woff2"); font-weight: 800 900; font-display: block; }
@font-face { font-family: "Noto Sans CJK KR"; src: url("public/fonts/NotoSansKR-Bold.woff2") format("woff2"); font-weight: 700; font-display: block; }
@font-face { font-family: "Noto Sans CJK KR"; src: url("public/fonts/NotoSansKR-Black.woff2") format("woff2"); font-weight: 800 900; font-display: block; }
@font-face { font-family: "Noto Sans JP"; src: url("public/fonts/NotoSansKR-Bold.woff2") format("woff2"); font-weight: 700; font-display: block; }
```

`body`엔 `font-family: "Noto Sans CJK KR", "Noto Sans KR", "Noto Sans JP", sans-serif; -webkit-font-smoothing: antialiased;`.

---

## 3. 세로 3분할 레이아웃

| 밴드 | 위치 | 크기 | 역할 | z-index |
|---|---|---|---|---|
| 진행바 `.progress` | `top: 0` | `1080×8` | 나레이션 종료 시각까지 채우는 리본 | **999** (최상단) |
| 타이틀바 `.titlebar` | `top: 0` | `1080×460` | 채널 아이콘 + 고정 제목 | 20 |
| 스테이지 `.stage` | `top: 460` | `1080×1060` | 씬별 배경 사진 (ken burns), **부가 텍스트/이모지 없음** | 10 |
| 자막바 `.capbar` | `bottom: 0` | `1080×400` | 자막 영역 | 20 |
| 자막 `.capline` | `bottom: 280` (in capbar) | `1080×auto` | 62px 볼드 텍스트 | 30 |

---

## 4. 타이틀바

### 4-1. 브랜드 아이콘
- 위치: `.brand { position: absolute; top: 40px; left: 60px; height: 160px; display: flex; align-items: center; gap: 22px; }`
- 아이콘: **160 × 160 원형** — `.icon { width:160px; height:160px; border-radius:50%; background-image: url("public/brand/channel-icon.png"); background-size:cover; }`
- 그림자: `box-shadow: 0 0 22px rgba(255,232,107,0.28), 0 4px 18px rgba(0,0,0,0.55);`
- **아이콘 옆 텍스트 없음** (아이콘만).
- 애니메이션(끝까지 일관되게 반복):
  ```js
  const pulseCycle = 2.8;
  const pulseRepeat = Math.max(1, Math.ceil(ROOT_DUR / pulseCycle) + 1);
  tl.set(".brand .icon", { scale: 0.96, opacity: 0.9, transformOrigin: "50% 50%" }, 0);
  tl.to(".brand .icon", {
    scale: 1.04, opacity: 1,
    duration: pulseCycle/2, ease: "sine.inOut",
    yoyo: true, repeat: pulseRepeat,
    transformOrigin: "50% 50%"
  }, 0);
  ```

### 4-2. 제목
- 컨테이너: `.title { position:absolute; top:224px; left:60px; right:60px; bottom:40px; display:flex; flex-direction:column; justify-content:center; align-items:center; text-align:center; }`
- 타이포: **font-size 82px, font-weight 900, letter-spacing -0.035em, line-height 1.08**
- 색: `#ffffff`, `text-shadow: 0 2px 12px rgba(0,0,0,0.7)`
- **두 줄 강제**: 각 줄을 `<span class="line">`로 감싼다. 한 줄에 한글 10~12자를 넘지 않게 조절.
- 강조어: `<span class="accent">` (색 `#ffe86b`)
- 예:
  ```html
  <div class="title">
    <span class="line">손가락에 물이 닿으면</span>
    <span class="line"><span class="accent">쭈글쭈글</span>해지는 이유</span>
  </div>
  ```
- **제목이 길어도 폰트 크기는 그대로 82px**. 대신 어절을 두 줄로 나누고 각 줄 길이를 조정. 82px에서 두 줄이 도저히 안 들어갈 정도로 길면 그때만 74px로 줄인다 (시리즈 일관성 우선).

---

## 5. 상단 진행바 (Progress)

```css
.progress {
  position: absolute; top: 0; left: 0;
  width: 1080px; height: 8px;
  background: linear-gradient(90deg, #ff3d68, #ffe86b);
  transform-origin: 0% 50%;
  transform: scaleX(0);
  z-index: 999;
}
```

```js
tl.to("#el-progress", { scaleX: 1, duration: narrationDuration, ease: "none" }, 0);
```

캔버스 **최상단**에 배치 (사진 위 겹치지 않도록 z-index 999). 나레이션 종료 시각까지 채워지고 그 후엔 그대로 100% 유지.

---

## 6. 색상 & 강조

| 역할 | 값 |
|---|---|
| 배경 | `#05060a` |
| 본문/제목 텍스트 | `#ffffff` |
| 강조 (`.accent`) | `#ffe86b` |
| 텍스트 그림자 | `0 2px 12px rgba(0,0,0,0.7)` |
| 아이콘 글로우 | `0 0 22px rgba(255,232,107,0.28)` |
| 진행바 그라디언트 | `#ff3d68 → #ffe86b` |

---

## 7. 씬 (스테이지 사진)

- 컨테이너: `.stage { position:absolute; top:460px; left:0; width:1080px; height:1060px; overflow:hidden; background:#05060a; }`
- 각 씬: `<div class="scene clip" data-start="…" data-duration="…" data-track-index="1|2">` (트랙 1·2 교차)
- 구성 요소는 **딱 세 개만**: `.bg` (사진) + `.tint` (톤 다운) + `.veil` (하단 어두운 그라디언트)
- **씬 위엔 절대 부가 텍스트·이모지 안 올린다** (kicker, stamp, hero 등 금지). 사진만.

```html
<div id="scene-N" class="scene clip" data-start="S" data-duration="D" data-track-index="1">
  <div data-layout-allow-overflow class="bg bg-N"></div>
  <div class="tint"></div>
  <div class="veil"></div>
</div>
```

CSS:

```css
.scene { position:absolute; top:0; left:0; width:1080px; height:1060px; opacity:0; }
.scene .bg { position:absolute; inset:0; background-size:cover; background-position:center; }
.scene .tint { position:absolute; inset:0;
  background: linear-gradient(180deg, rgba(0,0,0,0.15) 0%, rgba(0,0,0,0.25) 45%, rgba(0,0,0,0.72) 100%);
  z-index: 1;
}
.scene .veil { position:absolute; inset:0;
  background: linear-gradient(180deg, rgba(0,0,0,0) 55%, rgba(0,0,0,0.75) 100%);
  z-index: 2;
}
.bg-1 { background-image: url("public/bg/scene-1.jpg"); }
/* ... .bg-N */
```

Ken Burns 헬퍼:

```js
function sceneKB(sel, start, dur) {
  tl.to(sel, { opacity: 1, duration: 0.55, ease: "power2.out" }, start);
  tl.fromTo(sel + " .bg",
    { scale: 1.05, y: 20 },
    { scale: 1.14, y: -20, duration: dur, ease: "sine.inOut", transformOrigin: "50% 50%" },
    start);
  const outDur = Math.min(0.55, dur * 0.18);
  tl.to(sel, { opacity: 0, duration: outDur, ease: "power2.in" }, start + dur - outDur);
  tl.set(sel, { opacity: 0 }, 0);
}
```

`.bg`는 스케일이 캔버스 밖으로 나가므로 `data-layout-allow-overflow` 필수.

### 씬 개수·시간 배분

- 씬 개수: **6~9개** (~50s 나레이션 기준 9개가 자연스러움)
- 트랙 인덱스는 1과 2를 교차 (`1,2,1,2,…`)
- 각 씬 duration은 나레이션 문장 경계에 맞춤
- 마지막 씬 duration은 페이드아웃 종료 시각까지 이어지게 (root duration - 마지막 씬 start)

---

## 8. 사진 소싱 규칙

1. **저작권 무료 상업 사용 허용 소스만** — Unsplash, Pexels, Pixabay, Wikimedia Commons
2. Unsplash 직접 URL 패턴: `https://images.unsplash.com/photo-<ID>?w=1200&h=1200&fit=crop&crop=center&q=80`
3. 다운로드 후 `public/bg/scene-N.jpg`로 저장 (1200×1200 정사각형, CSS `background-size: cover`로 1080×1060에 채움)
4. AI 생성 이미지는 라이선스가 상업 사용을 허용하는 경우에만
5. 사진이 이상하다는 피드백을 받으면 해당 슬롯만 교체(전체 재작업 금지)

Unsplash 검색 페이지 예: `https://unsplash.com/s/photos/<keyword>?orientation=portrait`

---

## 9. 자막 (caption) — 시리즈 서체·크기·규칙

### 9-1. 스타일

```css
.capbar { position:absolute; bottom:0; left:0; width:1080px; height:400px; background:#05060a; z-index:20; }
.capline {
  position: absolute; left: 0; right: 0; bottom: 280px;
  text-align: center;
  font-size: 62px; font-weight: 800;
  line-height: 1.22; letter-spacing: -0.025em;
  color: #fff;
  white-space: nowrap; word-break: keep-all;
  text-shadow: 0 2px 12px rgba(0,0,0,0.7);
  opacity: 0;
  z-index: 30;
}
.capline .accent { color: #ffe86b; }
```

### 9-2. 텍스트 규칙 (엄격)

- **원본 스크립트 그대로**. 요약·의역·단어 삭제·조사 변경 금지.
- 나레이션에서 발화되는 모든 문장은 자막으로 화면에 나와야 한다 (silence gap 금지).
- **자막 컷은 사용자가 넘긴 스크립트의 줄바꿈을 그대로 따른다** — 한 줄 = 한 자막 클립. 임의로 더 쪼개거나 합치지 않는다. (한 줄이 화면에서 넘칠 때만 어절 경계에서 최소 분할)
- 접속어("그럼", "그러니까", "이렇게" 등)와 조사 원문 유지.
- **물음표(`?`)만 원본 스크립트에 있는 그대로 유지**하고, 나머지 문장부호(`.`, `,`, `!`, `…` 등)는 자막 텍스트에서 전부 제거한다. 원본 스크립트에 있어도 `?` 이외에는 자막에 절대 넣지 않는다.

### 9-3. 타이밍

- Whisper word-level 트랜스크립트 기준으로 잡되, 텍스트는 원본 스크립트 그대로.
- 클립 아이디: `cap-0`, `cap-1`, … 순차 부여
- 트랙 인덱스는 **3과 4를 번갈아** (`cap-0 → 3, cap-1 → 4, cap-2 → 3, …`) 붙는 자막 겹침 방지
- 각 자막 duration: `다음 자막 start - 현재 start - 0.02s` (미세 간극)

### 9-4. 애니메이션 (개별 자막)

```js
document.querySelectorAll(".capline").forEach((el) => {
  const start = parseFloat(el.getAttribute("data-start"));
  const dur   = parseFloat(el.getAttribute("data-duration"));
  const inDur  = Math.max(0.18, Math.min(0.3, dur * 0.25));
  const outDur = Math.max(0.14, Math.min(0.25, dur * 0.2));
  tl.set(el, { opacity: 0, y: 14 }, 0);
  tl.to(el, { opacity: 1, y: 0, duration: inDur, ease: "power2.out" }, start);
  tl.to(el, { opacity: 0, y: -8, duration: outDur, ease: "power2.in" }, start + dur - outDur);
});
```

**ID 하드코딩 금지** — `querySelectorAll('.capline').forEach` 만 사용.

---

## 10. 오디오

### 10-1. 나레이션

> ⛔ **나레이션 트랙에는 `afade` · 볼륨 감쇠·페이드인·페이드아웃을 절대 걸지 않는다.** 오디오 처리는 여기 명시된 loudnorm + head-boost + tail-boost **파이프라인 딱 하나로 끝**. 뒤에 어떤 볼륨 자동화도 붙이지 않는다. 페이드는 §10-2의 BGM에만.

- 파일: `narration.wav` (48kHz, 16-bit, stereo)
- **정규화 + 앞부분 head-boost + 뒷부분 tail-boost를 한 번의 ffmpeg 파이프라인으로 반드시 함께 적용한다.** 이 시리즈 나레이터는 **첫 1초가 눈에 띄게 조용하고**, 문장 끝으로도 갈수록 목소리가 급격히 작아지는 특성이 있어, 두 부스트 없이 렌더하면 시작과 결말이 잘 안 들린다. 절대 생략 금지. 정규화는 `dynaudnorm`이 아니라 **`loudnorm`(EBU R128)** 을 쓴다 — dynaudnorm은 look-ahead 특성상 첫 프레임을 제대로 못 잡아서 시작 볼륨이 뭉개진다.
  ```bash
  ffmpeg -y -i /home/user/First/<나레이션>.wav \
    -af "loudnorm=I=-16:TP=-1.5:LRA=11,volume='if(lt(t,0.8),2.0,if(lt(t,2.0),2.0-(t-0.8)*(1/1.2),if(gte(t,N_END-6),1.55,if(gte(t,N_END-8),1+(t-(N_END-8))/2*0.55,1))))':eval=frame" \
    -ar 48000 -ac 2 -c:a pcm_s16le narration.wav
  ```
  - `N_END` = 나레이션 총 길이(초). 예: 52.28s → `N_END-6=46.28`, `N_END-8=44.28`.
  - Head-boost: 0~0.8s **+6dB(×2.0)** 고정, 0.8~2.0s 선형 램프 다운 → 1.0x.
  - Tail-boost: `N_END-8`s 램프 시작, `N_END-6`s 이후 **+55%(×1.55)** 고정.
  - 검증: `ffmpeg -ss 0 -t 1 -i narration.wav -af volumedetect -f null -` 의 `mean_volume`이 중간 구간(예: `-ss 20 -t 1`)과 **±3dB 이내**여야 한다. 벗어나면 head-boost 배수를 키운다.
- 시작 지연: 항상 나레이션이 0초부터 시작하도록 (앞부분 무음 잘라내기)
- 태그:
  ```html
  <audio id="el-narration" class="clip" src="narration.wav"
         data-start="0" data-duration="<narration_len>"
         data-track-index="10" data-volume="1.0"></audio>
  ```

### 10-2. BGM

- 파일: `public/bgm/wildfire-instrumental.m4a` (또는 시리즈 지정 곡)
- **사용자가 올린 BGM mp3는 이미 보컬 없는 반주 파일** — 별도 vocal-removal 가공 금지.
- `ffmpeg -i src.mp3 -c:a aac -b:a 192k public/bgm/<곡명>-instrumental.m4a` 로 컨테이너만 변환
- 볼륨: `data-volume="0.5376"` (rain 영상 최종 0.672 × 0.8 = 0.5376)
- duration은 root와 일치 (60s 반주가 55s 영상보다 길면 자연스럽게 컷됨)
- **BGM에 afade in/out을 반드시 baked in — 시작 4s fade-in, 마지막 4s fade-out.** 나레이션은 절대 건드리지 말고 오직 BGM 파일만 가공. 프로젝트별 root duration에 맞춰 fade-out 시작점을 계산한다 (`root_dur - 4`).
  ```bash
  # 예: root_dur=55.08s → fade-out 시작=51.08s
  ffmpeg -y -i public/bgm/<곡명>-instrumental.m4a \
    -af "afade=t=in:st=0:d=4,afade=t=out:st=<root_dur-4>:d=4" \
    -c:a aac -b:a 192k /tmp/bgm-faded.m4a
  mv /tmp/bgm-faded.m4a public/bgm/<곡명>-instrumental.m4a
  ```
  - 검증: `ffmpeg -ss 0 -t 0.5 -i <bgm> -af volumedetect -f null -` mean_volume이 t=0에서 t=4로 뚜렷하게 상승, t=root_dur-4에서 t=root_dur로 뚜렷하게 하강해야 한다.
  - 나레이션 트랙(10번)에는 afade **절대 걸지 않는다** — head-boost/tail-boost는 10-1의 loudnorm 파이프라인 하나로 끝.
- 태그:
  ```html
  <audio id="el-bgm" class="clip" src="public/bgm/wildfire-instrumental.m4a"
         data-start="0" data-duration="<root_dur>"
         data-track-index="11" data-volume="0.5376"></audio>
  ```

트랙 인덱스: 나레이션 10, BGM 11.

---

## 11. 페이드아웃

```html
<div id="el-fadeout" class="fadeout clip"
     data-start="<narration_end - 1.2>" data-duration="4.0"
     data-track-index="99"></div>
```

```css
.fadeout { position: absolute; inset: 0; background: #000; opacity: 0; z-index: 100; }
```

```js
tl.fromTo("#el-fadeout",
  { opacity: 0 },
  { opacity: 1, duration: 4.0, ease: "power2.inOut" },
  narrationEnd - 1.2);
```

**root duration = 페이드아웃 종료 시각**과 정확히 일치. 남기면 마지막 씬이 다시 튀어나옴.

---

## 12. 프로젝트 구조

```
videos/<주제>-shorts/
  index.html                  # 메인 컴포지션 (단일 파일)
  meta.json                   # { id, name }
  package.json                # hyperframes CLI 스크립트
  narration.wav               # 정규화된 나레이션
  transcript.json             # Whisper word-level 결과
  public/
    bg/scene-1.jpg ... scene-N.jpg   # 씬 배경 사진 (Unsplash CC0)
    bgm/<곡명>-instrumental.m4a       # 반주 BGM
    brand/channel-icon.png            # 잡지식탁 아이콘 (루트의 .png 복사)
    fonts/NotoSansKR-Black.woff2     # 임베드 폰트
    fonts/NotoSansKR-Bold.woff2
    vendor/gsap.min.js                # gsap@3.14.2
  renders/
    video.mp4              # 원본 렌더
    video-upload.mp4       # 업로드용 (CRF 22)
    video-compressed.mp4   # 프리뷰용 (540×960, CRF 30)
```

> **썸네일은 렌더 영상에서 뽑지 않는다.** 첫 씬 프레임을 추출하지도, `renders/thumbnail.jpg`를 만들지도 말 것. 대신 `public/bg/scene-N.jpg` 중 주제를 가장 잘 대표하는 원본 사진(보통 씬 1)을 사용자에게 그대로 전달한다.

---

## 13. 워크플로 (시리즈 새 편 만들 때)

```bash
# 1. 스캐폴드
cd videos
npx hyperframes@0.8.10 init <주제>-shorts \
  --example blank --resolution portrait \
  --audio /home/user/First/<나레이션.wav> \
  --skip-transcribe --non-interactive --skip-skills

cd <주제>-shorts

# 2. 자산 준비
mkdir -p public/{bg,bgm,brand,fonts,vendor} renders
cp /home/user/First/잡지식탁_아이콘_260815.png public/brand/channel-icon.png
cp ../wrinkled-fingers-shorts/public/fonts/*.woff2 public/fonts/
cp ../wrinkled-fingers-shorts/public/vendor/gsap.min.js public/vendor/

# 3. BGM (사용자가 올린 반주 mp3 사용) + afade in/out baked in (필수)
ffmpeg -y -i /home/user/First/<BGM>.mp3 -c:a aac -b:a 192k \
  public/bgm/<곡명>-instrumental.m4a
# ↓ root_dur 확정된 뒤에 반드시 실행 (fade-out st = root_dur - 4)
ffmpeg -y -i public/bgm/<곡명>-instrumental.m4a \
  -af "afade=t=in:st=0:d=4,afade=t=out:st=<root_dur-4>:d=4" \
  -c:a aac -b:a 192k /tmp/bgm-faded.m4a
mv /tmp/bgm-faded.m4a public/bgm/<곡명>-instrumental.m4a

# 4. 나레이션 loudnorm + head-boost + tail-boost (**필수** — 시작/끝 볼륨 급감 방지)
#    N_END = 나레이션 총 길이(초)를 반드시 반영
ffmpeg -y -i /home/user/First/<나레이션>.wav \
  -af "loudnorm=I=-16:TP=-1.5:LRA=11,volume='if(lt(t,0.8),2.0,if(lt(t,2.0),2.0-(t-0.8)*(1/1.2),if(gte(t,N_END-6),1.55,if(gte(t,N_END-8),1+(t-(N_END-8))/2*0.55,1))))':eval=frame" \
  -ar 48000 -ac 2 -c:a pcm_s16le narration.wav

# 5. 트랜스크립트
npx hyperframes@0.8.10 transcribe narration.wav --model small --language ko --json

# 6. Unsplash에서 씬별 사진 다운로드 → public/bg/scene-N.jpg

# 7. index.html 작성 — 최신 규칙이 모두 반영된 참조 편을 복사한 뒤 텍스트·타이밍·bg-N만 교체
#    ✓ 승인 참조: mosquito-attraction-shorts/index.html (2026-09-06 이후 규칙 모두 반영)
#    ✗ 오래된 참조(wrinkled-fingers-shorts/index.html)에는 옛 규칙이 남아있을 수 있음 — 그걸 복사할 때는
#      §9-2, §10-1, §10-2 규칙을 직접 재검토해야 함

# 8. 사전 검증 — 절대 금기 3종 감시 (⛔ 아무거나 하나라도 걸리면 렌더 금지)
#    (a) narration 트랙에 afade가 안 붙었는지
grep -E 'id="el-narration"[^>]*' index.html | grep -Ei 'afade|fade|volume-automation' \
  && { echo "❌ narration에 페이드/볼륨 자동화 발견 — 제거 후 재시도"; exit 1; }
#    (b) BGM 파일에 fade-in/out이 실제 baked in 됐는지 (t=0에서 -25dB 이하여야 정상 fade-in)
BGM_HEAD=$(ffmpeg -ss 0 -t 0.5 -i public/bgm/*.m4a -af volumedetect -f null - 2>&1 | \
  awk '/mean_volume:/ {print $5}')
awk -v v=$BGM_HEAD 'BEGIN{exit !(v > -25)}' \
  && { echo "❌ BGM에 fade-in 미적용 (t=0 mean=$BGM_HEAD dB) — §10-2 재실행"; exit 1; }
#    (c) narration 첫 1s vs 중간 1s 볼륨 편차 ≤ 3dB (head-boost 검증)
H=$(ffmpeg -ss 0 -t 1 -i narration.wav -af volumedetect -f null - 2>&1 | awk '/mean_volume:/ {print $5}')
M=$(ffmpeg -ss 20 -t 1 -i narration.wav -af volumedetect -f null - 2>&1 | awk '/mean_volume:/ {print $5}')
awk -v h=$H -v m=$M 'BEGIN{d=h-m; if(d<0)d=-d; exit !(d<=3)}' \
  || { echo "❌ narration head-boost 부족 (t=0: $H dB vs t=20: $M dB) — §10-1 재실행"; exit 1; }

# 9. 검증 + 렌더
npx hyperframes@0.8.10 check
npx hyperframes@0.8.10 render -q standard -o renders/video.mp4

# 10. 배포용 인코딩
ffmpeg -y -i renders/video.mp4 -c:v libx264 -crf 22 -preset medium -pix_fmt yuv420p \
  -c:a aac -b:a 128k renders/video-upload.mp4
ffmpeg -y -i renders/video.mp4 -vf scale=540:960 -c:v libx264 -crf 30 -preset medium \
  -pix_fmt yuv420p -c:a aac -b:a 96k renders/video-compressed.mp4

# ⛔ 렌더 프레임 추출 금지 — `ffmpeg -ss ... -frames:v 1 renders/thumbnail.jpg` 명령을 실행하지 않는다.
#    렌더 후 `[ -e renders/thumbnail.jpg ] && rm renders/thumbnail.jpg` 로 실수 방지.
[ -e renders/thumbnail.jpg ] && rm renders/thumbnail.jpg
# 썸네일 소스는 첫 씬 원본 사진 그대로 사용자에게 전송한다 (예: public/bg/scene-1.jpg).
```

---

## 14. 시리즈 일관성 체크리스트 (렌더 전에 항상 확인)

- [ ] 캔버스 1080×1920, 30fps
- [ ] `public/fonts/NotoSansKR-{Black,Bold}.woff2` 임베드됨
- [ ] 타이틀바 460px, 진행바 top:0/height:8/z:999, 스테이지 460~1520, 자막바 하단 400px
- [ ] 브랜드 아이콘 160×160 원형, 좌상단, **옆 텍스트 없음**
- [ ] 아이콘 펄스 root duration 끝까지 반복 (`Math.ceil(rootDur/2.8)+1`)
- [ ] 제목 82px / weight 900 / letter-spacing -0.035em / line-height 1.08 / 두 줄
- [ ] 강조어 `.accent` 색 `#ffe86b`
- [ ] 자막 62px / weight 800 / letter-spacing -0.025em / white-space nowrap
- [ ] **자막 컷 = 원본 스크립트 줄바꿈 그대로** (한 줄 = 한 클립, 임의 분할·합치기 금지)
- [ ] **자막 텍스트에서 물음표(`?`)만 유지**, 나머지 문장부호(`.` `,` `!` `…` 등)는 모두 제거
- [ ] 자막 트랙 3/4 교차, 루프는 `.capline` 클래스 순회
- [ ] 씬 스테이지 위에 **부가 텍스트·이모지 없음** — 사진 + tint + veil만
- [ ] 씬 배경 사진 Unsplash CC0 (라이선스 확인)
- [ ] 나레이션 트랙 10, BGM 트랙 11, BGM 볼륨 0.5376
- [ ] **나레이션에 head-boost + tail-boost 둘 다 적용됨** (loudnorm 뒤에 volume 필터 체이닝: 0~0.8s ×2.0 → 2.0s까지 램프 다운, 마지막 8s부터 램프 시작, 6s 이후 ×1.55)
- [ ] **나레이션 첫 1s vs 중간 1s의 `mean_volume` 차이가 ±3dB 이내** (`ffmpeg -ss T -t 1 -af volumedetect`로 검증)
- [ ] BGM은 이미 반주 파일 그대로 사용, 별도 vocal-removal 하지 않음
- [ ] **BGM에 afade in/out 4s baked in** (fade-in 0~4s, fade-out `root_dur-4`~`root_dur`). 나레이션엔 절대 afade 걸지 않음
- [ ] 페이드아웃 4s, root duration = 페이드아웃 종료 시각
- [ ] `.stage`, `.titlebar`, `.capbar`, `#el-progress`, BGM의 `data-duration`이 root와 일치
- [ ] `npm run check` 에러 0개
- [ ] **`renders/thumbnail.jpg` 존재 금지** (렌더 영상에서 프레임 추출 금지)
- [ ] 썸네일 소스는 `public/bg/scene-N.jpg` 중 주제 대표 원본 한 장을 사용자에게 전달

---

## 15. 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| 렌더가 성공 로그를 냈는데 mp4 파일이 갱신 안 됨 | 하이퍼프레임즈의 ffmpeg 감지 오류 (간헐적) | `npx hyperframes render` 재실행 |
| 자막 서체가 이전 편과 다름 | 폰트 파일 임베드 누락 → 시스템 폰트 폴백 | `public/fonts/`에 woff2 넣고 `@font-face`에 `url(...)` 지정 |
| 마지막 씬이 페이드 후 다시 튀어나옴 | root duration이 페이드 종료 시각보다 김 | root duration을 페이드 종료 시각과 정확히 같게 |
| 자막이 화면 밖으로 넘침 | 한 줄이 17자 초과 | 어절 경계에서 컷 분할 |
| 사진이 씬 duration보다 느리게 확대됨 | ken burns 스케일 target이 너무 큼 | 1.14로 유지, y 이동은 ±20 |
| GSAP `target not found` 경고 | 삭제된 선택자를 애니메이션이 참조 | `sceneKB`에서 없어진 하위 요소 애니메이션 제거 |
| 뒷부분 나레이션 목소리가 작음 | 나레이터 볼륨 자연 감소 | `volume` 필터에 시간 램프 적용 (10-1 참고) |

---

## 16. 참고: 첫 편(wrinkled-fingers-shorts)의 실제 값

- 제목: "손가락에 물이 닿으면 / **쭈글쭈글**해지는 이유"
- 나레이션 길이: 52.55s
- root duration: **55.35s** (52.55 - 1.2 + 4.0)
- 페이드아웃 시작: 51.35s, duration 4.0s
- 씬 개수: 9개
- 자막 개수: 30개
- BGM: `wildfire-instrumental.m4a` 60s (사용자가 올린 반주 파일)
