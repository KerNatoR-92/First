# 잡지식탁 쇼츠 썸네일 템플릿 (blanket-smell 기반)

> 트리거: **"썸네일 만들어줘"** / **"이전에 만들었던 스타일로 썸네일"**
> 짝이 되는 영상 템플릿: `잡지식탁-쇼츠-템플릿.md`.
> 참조 산출물: `videos/<프로젝트>-shorts/thumbnail.html`, `videos/<프로젝트>-shorts/public/thumbnail.jpg`.
> 원형: 이전 blanket-smell / rain-smell 시리즈 썸네일 (미니 브랜드칩 좌상단 + 좌정렬 대형 두 줄 타이틀).

---

## 1. 캔버스

- 해상도: **1080 × 1920** (세로 쇼츠 / 유튜브 쇼츠 커버)
- 포맷: **JPG**, 품질 92 (파일 150~250KB 목표)
- 파일 위치: `videos/<프로젝트>-shorts/public/thumbnail.jpg`
- 원본 HTML: `videos/<프로젝트>-shorts/thumbnail.html` (버전 관리 대상)
- 폰트: **Noto Sans KR** (`public/vendor/NotoSansKR-{Regular,Bold,Black}.woff2`)
- 배경 컬러 폴백: `#05060a`
- 스무딩: `-webkit-font-smoothing: antialiased`

## 2. 색상 (영상 템플릿 §3와 동일 팔레트)

- 배경 컬러: `#05060a`
- 본문/제목: `#ffffff`
- 강조색 (`.accent`): `#ffe86b` — **제목의 핵심 단어에만 사용** (한 단어)
- 브랜드 도트 색: `#ffe86b`
- 텍스트 그림자: `0 3px 20px rgba(0,0,0,0.85), 0 0 40px rgba(0,0,0,0.5)` (제목),  `0 2px 12px rgba(0,0,0,0.7)` (브랜드)

## 3. 레이아웃 (2개 존만)

**미니멀 구성.** eyebrow·bottom hook 같은 부가 카피는 없음 — 시선을 대형 두 줄 타이틀 하나에 집중.

| 존 | 위치 | 역할 |
|---|---|---|
| 좌상단 브랜드칩 | `top: 60, left: 80` | 노랑 도트 + "잡지식탁" 워드마크 |
| 좌정렬 대형 타이틀 | `top: 44%` (translateY 없음, 위 기준선) | 두 줄, 강조어 노랑 |

### 배경

- `<div class="bg">` : 프로젝트 첫 씬 이미지(`public/bg/01.jpg`)를 `background-size: cover; background-position: center`로 풀-블리드
- `<div class="veil">` : 강한 어둠 오버레이 gradient (텍스트 판독성 최우선, 배경은 mood로만 남김)
  - `linear-gradient(180deg, rgba(5,6,10,0.78) 0%, rgba(5,6,10,0.60) 40%, rgba(5,6,10,0.68) 70%, rgba(5,6,10,0.82) 100%)`

### 좌상단 브랜드칩 (핵심 변경 — 영상 아이콘과 다름)

- `.brand` : `top: 60, left: 80`, `display: flex; align-items: center; gap: 20`
- `.dot` : **22×22 원형 노랑 점**
  - `background: #ffe86b`, `border-radius: 50%`
  - `box-shadow: 0 0 12px rgba(255,232,107,0.5)` (은은한 글로우)
  - **영상의 160×160 원형 아이콘 사진과 다름.** 썸네일은 이미지가 시선 압도자이므로 브랜드는 최소화 (도트+텍스트 칩).
- `.wm` : "잡지식탁"
  - `font-weight: 900, font-size: 42px, letter-spacing: -0.02em`, 흰색
  - `text-shadow: 0 2px 12px rgba(0,0,0,0.7)`

### 좌정렬 대형 타이틀

- `.title-block` : `left: 80, right: 80, top: 44%`
  - `display: flex; flex-direction: column; align-items: flex-start; text-align: left`
  - **좌정렬**. 중앙정렬 아님 — 참조 스타일 그대로.
- `.line` : 두 줄, 각 줄을 `<div class="line">`로 감싸기
  - `font-weight: 900, font-size: 148px, line-height: 1.02, letter-spacing: -0.045em`
  - `white-space: nowrap; word-break: keep-all` — 두 줄 강제, 중간 개행 방지
  - 흰색 기본, 강조어에 `<span class="accent">` (노랑)
  - `.line + .line { margin-top: 6px }`
- **폰트 크기 자동 조정**: 한 줄 폭이 920px(=1080-2·80)를 넘으면 카피를 줄이거나 폰트를 132~148px 사이에서 낮춤. 절대 자동 개행 시키지 말 것.

## 4. 카피 작성 원칙

- **총 두 줄, 각 줄 5~9자**. 참조: "새 이불에서 왜 / 냄새가 날까?" (7자 / 6자).
- **첫 줄**: 상황/조건 + "왜" 로 끝남 (질문 톤 유지).
- **두 번째 줄**: 주어(강조어) + 서술어 + `?` — 예: "냄새가 날까?", "거울이 뿌예질까?", "머리가 지끈할까?"
- **강조어**: 두 번째 줄의 **핵심 명사 하나만**. 색은 노랑(`.accent`).
  - 참조: "냄새", "거울", "머리" 같은 주제어.
  - 절대 "왜"에 강조 걸지 말 것. 강조는 궁금증의 대상(what)에.
- **영상 타이틀과 100% 일치할 필요는 없음** — 썸네일용으로 축약/변형 가능. 참조 스타일(blanket-smell)도 영상 타이틀보다 짧음.
- 여러 카피 후보를 만들 때 우선순위: (1) 각 줄 ≤ 9자 (2) 첫 줄 끝 "왜" (3) 강조어는 명사.

### 이번 프로젝트(mirror-fog) 실제 카피

- Line 1: `샤워 뒤에 왜`
- Line 2: `<accent>거울</accent>이 뿌예질까?`

## 5. 파이프라인 (썸네일 만들기)

프로젝트 안에 다음 두 파일이 있어야 함:

1. `thumbnail.html` — 이 문서의 §3 구조대로 작성
2. `scripts/render-thumbnail.mjs` — Playwright + 로컬 Chromium으로 1080×1920 스크린샷

### `scripts/render-thumbnail.mjs` (표준)

```js
#!/usr/bin/env node
import { chromium } from "playwright";
import { resolve } from "node:path";
import { pathToFileURL } from "node:url";

const projectRoot = resolve(new URL(import.meta.url).pathname, "..", "..");
const htmlPath = resolve(projectRoot, "thumbnail.html");
const outPath  = resolve(projectRoot, "public", "thumbnail.jpg");

const executablePath = process.env.PLAYWRIGHT_BROWSER_EXEC
  || "/opt/pw-browsers/chromium-1194/chrome-linux/chrome";

const browser = await chromium.launch({ executablePath });
try {
  const ctx = await browser.newContext({
    viewport: { width: 1080, height: 1920 },
    deviceScaleFactor: 1,
  });
  const page = await ctx.newPage();
  await page.goto(pathToFileURL(htmlPath).href, { waitUntil: "networkidle" });
  await page.evaluate(() => document.fonts.ready);
  await page.waitForTimeout(300);
  await page.screenshot({ path: outPath, type: "jpeg", quality: 92, fullPage: false });
  console.log(`✓ wrote ${outPath}`);
} finally {
  await browser.close();
}
```

- Chromium 경로가 다르면 `PLAYWRIGHT_BROWSER_EXEC` 환경변수로 오버라이드.
- `document.fonts.ready` + `waitForTimeout(300)` 으로 woff2 로딩·렌더 안정화.

### 실행

```bash
cd videos/<프로젝트>-shorts
node scripts/render-thumbnail.mjs
# → public/thumbnail.jpg 생성
```

## 6. 배경 이미지 선택 규칙

- 기본: **프로젝트 첫 씬 배경**(`public/bg/01.jpg`)을 그대로 사용.
- 첫 씬이 인물·문자·산만한 요소가 있어 텍스트 판독을 방해하면 다른 씬 이미지로 교체 (`bg/03.jpg` 등).
- 후보가 없으면 solid `#05060a` + 액센트 SVG 요소로 대체 가능 (fallback).
- 배경은 어차피 veil이 강하게 덮으므로 완벽할 필요 없음 — mood만 잡히면 OK.

## 7. 유튜브 업로드 가이드

- 파일: `public/thumbnail.jpg` 다운로드 → 유튜브 스튜디오 "미리보기 이미지" 업로드
- 크기: 1080×1920 세로. 쇼츠는 스튜디오에서 자동으로 세로 커버로 표시
- 용량 상한: 2MB (유튜브 정책). 이 템플릿은 150~250KB 나옴.

## 8. 체크리스트

- [ ] 해상도 정확히 1080×1920인가?
- [ ] 파일 크기 2MB 이하?
- [ ] 좌상단 브랜드칩이 노랑 도트 + "잡지식탁" 워드마크 조합인가? (사진 아이콘 아님)
- [ ] 타이틀이 두 줄, **좌정렬**, 148px(±10), `white-space: nowrap`인가?
- [ ] 각 줄이 5~9자 이내이고 자동 개행이 안 일어나는가?
- [ ] 첫 줄이 "왜"로 끝나고, 강조어(`.accent`)는 두 번째 줄의 **핵심 명사 하나만**인가?
- [ ] 배경 veil이 충분히 진해서 텍스트가 확실히 판독되는가?
- [ ] eyebrow·bottom hook 같은 부가 카피는 없는가? (미니멀 유지)
