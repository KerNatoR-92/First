# 잡지식탁 쇼츠 썸네일 템플릿

> 트리거: **"썸네일 만들어줘"** / **"이전에 만들었던 스타일로 썸네일"**
> 짝이 되는 영상 템플릿: `잡지식탁-쇼츠-템플릿.md`.
> 참조 산출물: `videos/<프로젝트>-shorts/thumbnail.html`, `videos/<프로젝트>-shorts/public/thumbnail.jpg`.

---

## 1. 캔버스

- 해상도: **1080 × 1920** (세로 쇼츠 / 유튜브 쇼츠 커버)
- 포맷: **JPG**, 품질 92 (파일 200~400KB 목표)
- 파일 위치: `videos/<프로젝트>-shorts/public/thumbnail.jpg`
- 원본 HTML: `videos/<프로젝트>-shorts/thumbnail.html` (버전 관리 대상)
- 폰트: **Noto Sans KR** (`public/vendor/NotoSansKR-{Regular,Bold,Black}.woff2`)
- 배경 컬러 폴백: `#05060a`
- 스무딩: `-webkit-font-smoothing: antialiased`

## 2. 색상 (영상 템플릿 §3와 동일)

- 배경 컬러: `#05060a`
- 본문/제목: `#ffffff`
- 강조색 (`.accent`): `#ffe86b` — **제목·서브훅에서만 사용**
- 텍스트 그림자: `0 3px 20px rgba(0,0,0,0.85), 0 0 40px rgba(0,0,0,0.5)` (제목),  `0 2px 12px rgba(0,0,0,0.7)` (그 외)

## 3. 레이아웃 (3개 존)

썸네일은 정지 이미지이므로 영상의 3분할 밴드 대신 **배경 풀-블리드 + 3개 텍스트 존** 구조:

| 존 | 위치 | 역할 |
|---|---|---|
| 상단 브랜드 | `top: 60, left: 60` | 아이콘 160×160 + 워드마크 |
| 중앙 제목 | `top: 50%` (translateY -50%) | 오늘의 궁금증 + 두 줄 제목 |
| 하단 훅 | `bottom: 180` | 답/키워드 한 줄 (강조어 노랑) |

### 배경

- `<div class="bg">` : 프로젝트 첫 씬 이미지(`public/bg/01.jpg`)를 `background-size: cover; background-position: center`로 풀-블리드
- `<div class="veil">` : 위→아래 gradient 어두운 오버레이 (텍스트 판독성)
  - 위쪽: `rgba(5,6,10,0.62)` — 상단 브랜드 배경 확보
  - 25%: `rgba(5,6,10,0.42)` — 배경 이미지 살짝 노출
  - 55%: `rgba(5,6,10,0.72)` — 제목 뒤 어둡게
  - 하단: `rgba(5,6,10,0.88)` — 훅 확실히 판독

### 상단 브랜드

- `.brand` : `top: 60, left: 60`, flex row, `gap: 22`
- `.icon` : **160×160 원형** (영상 템플릿 §2와 동일 크기)
  - `border-radius: 50%`, `object-fit: cover`
  - `box-shadow: 0 0 22px rgba(255,232,107,0.35), 0 4px 18px rgba(0,0,0,0.65)`
  - 소스: `public/brand/channel-icon.png`
- `.wm` : **아이콘 옆 워드마크 있음** (썸네일 한정 — 영상은 없음)
  - `font-weight: 900, font-size: 52, letter-spacing: -0.02em`, 흰색
  - 이유: 정지 이미지라 채널명이 한눈에 들어와야 함. 영상에선 아이콘만.

### 중앙 제목

- `.title-block` : 화면 정중앙 (`top: 50%; transform: translateY(-50%)`)
- `.eyebrow` : 상단 미니 카피 (예: `"오늘의 궁금증"`)
  - `font-weight: 700, font-size: 44, letter-spacing: 0.28em, uppercase`
  - **색상: 노랑(`#ffe86b`)** — 브랜드 액센트로 시선 유도
  - `margin-bottom: 26`
- `.line` : 두 줄 제목, 각 줄 `<span class="line">` 로 감싸기
  - `font-weight: 900, font-size: 154, line-height: 1.02, letter-spacing: -0.045em`
  - 흰색 기본, 강조어에 `<span class="accent">` (노랑)
  - 두 줄 강제 (`flex-direction: column`, `align-items: center`, `text-align: center`)
  - 서체 크기는 **142~158px 범위**에서 라인 글자수에 맞춰 조정 (짧으면 크게, 길면 작게)

### 하단 훅

- `.bottom-hook` : `bottom: 180`, 가운데 정렬
- `font-weight: 800, font-size: 62, letter-spacing: -0.02em`, 흰색
- 강조어에 `.accent` (노랑) — 영상 핵심 키워드/정답 (예: `"응결"`, `"뜨거운 물"`)
- 역할: 제목이 질문이면 훅은 힌트/티저. 미리보기 클릭 유도.

## 4. 카피 작성 원칙

- **제목**: 영상 타이틀과 완전히 일치시켜 CTR 시너지 (본편 브랜드 인지). 두 줄 강제.
- **eyebrow**: `"오늘의 궁금증"`, `"매일의 잡학"`, `"3줄 요약"` 등 시리즈 라벨. 짧게 (4~8자).
- **하단 훅**: 정답 한 줄 스포일러 or 힌트 (5~12자). 예:
  - 원리 영상 → `"정답은 <강조>키워드</강조> 현상"`
  - 팁 영상 → `"핵심은 <강조>실전 팁</강조>"`
  - 비교 영상 → `"결론: <강조>A vs B</강조>"`

## 5. 파이프라인 (썸네일 만들기)

프로젝트 안에 다음 두 파일이 있어야 함:

1. `thumbnail.html` — 이 문서의 §3 구조대로 작성
2. `scripts/render-thumbnail.mjs` — Playwright + 로컬 Chromium으로 1080×1920 스크린샷

### `scripts/render-thumbnail.mjs` (표준)

```js
#!/usr/bin/env node
import { chromium } from "playwright";
import { resolve } from "node:path";
import { pathToFileURL } from "node:url";

const projectRoot = resolve(new URL(import.meta.url).pathname, "..", "..");
const htmlPath = resolve(projectRoot, "thumbnail.html");
const outPath  = resolve(projectRoot, "public", "thumbnail.jpg");

const executablePath = process.env.PLAYWRIGHT_BROWSER_EXEC
  || "/opt/pw-browsers/chromium-1194/chrome-linux/chrome";

const browser = await chromium.launch({ executablePath });
try {
  const ctx = await browser.newContext({
    viewport: { width: 1080, height: 1920 },
    deviceScaleFactor: 1,
  });
  const page = await ctx.newPage();
  await page.goto(pathToFileURL(htmlPath).href, { waitUntil: "networkidle" });
  await page.evaluate(() => document.fonts.ready);
  await page.waitForTimeout(300);
  await page.screenshot({ path: outPath, type: "jpeg", quality: 92, fullPage: false });
  console.log(`✓ wrote ${outPath}`);
} finally {
  await browser.close();
}
```

- Chromium 경로가 다르면 `PLAYWRIGHT_BROWSER_EXEC` 환경변수로 오버라이드.
- `document.fonts.ready` + `waitForTimeout(300)` 으로 woff2 로딩·렌더 안정화.

### 실행

```bash
cd videos/<프로젝트>-shorts
node scripts/render-thumbnail.mjs
# → public/thumbnail.jpg 생성
```

## 6. 배경 이미지 선택 규칙

- 기본: **프로젝트 첫 씬 배경**(`public/bg/01.jpg`)을 그대로 사용.
- 첫 씬이 인물·문자·산만한 요소가 있어 텍스트 판독을 방해하면 다른 씬 이미지로 교체 (`bg/03.jpg` 등).
- 후보가 없으면 solid `#05060a` + 액센트 SVG 요소로 대체 가능 (fallback).

## 7. 유튜브 업로드 가이드

- 파일: `public/thumbnail.jpg` 다운로드 → 유튜브 스튜디오 "미리보기 이미지" 업로드
- 크기: 1080×1920 (2:16:9 세로) — 쇼츠는 스튜디오에서 자동으로 세로 커버로 표시
- 용량 상한: 2MB (유튜브 정책). 이 템플릿은 200~400KB 나옴.

## 8. 체크리스트

- [ ] 해상도 정확히 1080×1920인가?
- [ ] 파일 크기 2MB 이하?
- [ ] 브랜드 아이콘 160×160 좌상단에 있는가? 옆에 워드마크 있는가?
- [ ] 제목이 두 줄, 154px(±12) 크기, 흰색 + `.accent` 노랑 강조인가?
- [ ] eyebrow(짧은 라벨) 노랑, 대문자 스페이싱 넓은가?
- [ ] 하단 훅에 강조어(정답/키워드) 노랑으로 걸렸는가?
- [ ] 배경 veil이 아래로 갈수록 진해져서 하단 훅이 확실히 판독되는가?
- [ ] 영상 타이틀과 썸네일 제목이 일치하는가?
